using NUnit.Framework;

namespace ChatTests.Main
{
    [TestFixture]
	public class WikiTest
	{
        //[Test]
        //public void Test()
        //{
        //    var categoryIndex = GetCategoryIndex();
        //    foreach (var category in categoryIndex.GetSuggestedCategories("Comp").Take(10))
        //    {
        //        Debug.WriteLine(category.Name);

        //        foreach (var subItem in categoryIndex.GetChildCategories(category))
        //            Debug.WriteLine(category.Name + "." + subItem.Name);

        //        foreach (var subItem in categoryIndex.GetParentCategories(category))
        //            Debug.WriteLine(category.Name + ".." + subItem.Name);
				
        //        Debug.WriteLine("");
        //    }
        //}

        //private ICategoryService GetCategoryIndex()
        //{
        //    var index = new CategoryService(new CategoryFactoryService());
        //    using (var categoryReader = new WikipediaCategoryCursor())
        //        index.AddCategories(categoryReader);

        //    return index;
        //}
	}
}

